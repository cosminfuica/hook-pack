.agents
skills-lock.json
.claude/*.local.md
.claude/*.local.json
.claude
.kilo
.internal
docs/reference
docs/superpowers
node_modules/
# Build output: only the bundled dispatcher artifact ships in the repo so
# `/plugin install` works without running npm install / npm run build.
dist/*
!dist/hook-pack-dispatch.mjs
coverage/
.env
